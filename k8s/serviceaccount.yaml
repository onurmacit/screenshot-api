# =============================================================================
# Screenshot API - Service Account
# =============================================================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: screenshot-api
  namespace: screenshot-api
  labels:
    app: screenshot-api

---
# If using AWS IAM roles for service accounts (IRSA)
# apiVersion: v1
# kind: ServiceAccount
# metadata:
#   name: screenshot-api
#   namespace: screenshot-api
#   annotations:
#     eks.amazonaws.com/role-arn: arn:aws:iam::YOUR_ACCOUNT_ID:role/screenshot-api-role

